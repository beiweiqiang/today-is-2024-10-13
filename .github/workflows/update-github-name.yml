name: update-github-name-actions
on:
  schedule:
    - cron: '53 * * * *'
jobs:
  update-github-name:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install
      - run: npm start